# 🎲 Random Number Generator (React)

## 📌 Overview
The Random Number Generator is a simple React application that generates a random number between 1 and 100 when the user clicks a button. It uses React's `useState` hook to manage state and conditional rendering to display a placeholder message before any number is generated.

This project is ideal for beginners learning React fundamentals such as state, events, and dynamic UI updates.

---

## 🎯 Features
- Generates a random number between 1 and 100
- Uses React functional components
- Demonstrates `useState` for state management
- Uses conditional rendering
- Updates UI instantly on button click
- Simple and clean user interface

---

## 🛠️ Technologies Used
- React
- JavaScript (ES6)
- CSS

---

